services:
  - type: static
    name: iot-saas-dashboard
    env: static
    buildCommand: npm install && npm run build
    publishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
